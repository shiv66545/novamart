const products=NovaStore.products.map(p=>({...p,cat:p.category}));let cart=NovaStore.getCart(),category='All';const $=id=>document.getElementById(id),money=n=>NovaStore.money(n);function renderCats(){const cats=['All',...new Set(products.map(p=>p.cat))];$('cats').innerHTML=cats.map(c=>`<button class="${c===category?'active':''}" data-cat="${c}">${c}</button>`).join('');document.querySelectorAll('[data-cat]').forEach(b=>b.onclick=()=>{category=b.dataset.cat;renderCats();renderProducts()})}function renderProducts(){let q=$('search').value.toLowerCase(),list=products.filter(p=>(category==='All'||p.cat===category)&&p.name.toLowerCase().includes(q)),sort=$('sort').value;if(sort==='low')list.sort((a,b)=>a.price-b.price);if(sort==='high')list.sort((a,b)=>b.price-a.price);if(sort==='rating')list.sort((a,b)=>b.rating-a.rating);$('grid').innerHTML=list.map(p=>`<article class="card" onclick="location.href='products.html?id=${p.id}'"><button class="wish" onclick="event.stopPropagation();wish(${p.id})">${NovaStore.isWishlisted(p.id)?'♥':'♡'}</button><div class="pic">${p.icon}</div><div class="info"><span class="tag">${p.cat.toUpperCase()}</span><div class="name">${p.name}</div><div class="rating">★ ${p.rating}</div><div class="price">${money(p.price)}</div><button class="add" onclick="event.stopPropagation();add(${p.id})">Add to cart</button></div></article>`).join('');$('title').textContent=q?`Results for “${$('search').value}”`:category==='All'?'Trending products':category+' products'}function add(id){NovaStore.addToCart(id);cart=NovaStore.getCart();save();openCart()}function wish(id){NovaStore.toggleWishlist(id);renderProducts()}function save(){NovaStore.saveCart(cart);renderCart()}function renderCart(){let total=0;$('items').innerHTML=cart.length?cart.map(x=>{let p=products.find(y=>y.id===x.id);total+=p.price*x.qty;return `<div class="item"><div class="itemPic">${p.icon}</div><div class="itemInfo"><strong>${p.name}</strong><div>${money(p.price)} × ${x.qty}</div><div class="qty"><button onclick="change(${p.id},-1)">−</button>${x.qty}<button onclick="change(${p.id},1)">+</button></div></div></div>`}).join(''):'<p style="color:#6b7280;text-align:center;padding:50px 0">Your cart is empty.</p>';$('subtotal').textContent=money(total);$('count').textContent=cart.reduce((s,x)=>s+x.qty,0)}function change(id,n){NovaStore.updateQty(id,(cart.find(x=>x.id===id)?.qty||0)+n);cart=NovaStore.getCart();renderCart()}function openCart(){$('drawer').classList.add('open');$('overlay').classList.add('show')}function closeCart(){$('drawer').classList.remove('open');$('overlay').classList.remove('show')}$('cart').onclick=openCart;$('close').onclick=closeCart;$('overlay').onclick=closeCart;$('searchBtn').onclick=renderProducts;$('search').oninput=renderProducts;$('sort').onchange=renderProducts;$('shop').onclick=()=>document.querySelector('.products').scrollIntoView({behavior:'smooth'});renderCats();renderProducts();renderCart();